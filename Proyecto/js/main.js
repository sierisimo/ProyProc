$(document).ready(function(){
		//Obtiene todos los elementos canvas y selecciona el primero o de lo contrario se lanza por el que tenga el id "main"
		//var s = new SProc.System({container:"#main",canvas:{width:400,height:400}});
		//s.draw();
		//Revisamos que la variable no este undefined o nula.
		//!$canvas && return;
		
		//var server = new Queue.Server();
		//Queue.addServer();

		/* 
			Llamadas al singleton aqui.
		*/
	}
);